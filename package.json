{
  "name": "nant-es",
  "private": true,
  "scripts": {
		"dev:docs": "npm run dev --prefix ./docs",
		"build:docs": "npm run build --prefix ./docs"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.3",
    "lerna": "^8.1.3"
  }
}
