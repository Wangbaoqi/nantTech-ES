# Function Class



## Tail Position Calls

尾调用简单来说是在一个函数的尾部（一定是函数最后一步）调用另一个函数。

尾调用存在意义是为了减少调用栈大小，从而减少内存消耗。

一般来讲，每调用一次函数的时候，其上下文就会被记录在调用栈中，如果该函数调用结束且其中的变量没有被引用
时，其上下文就会从调用栈中弹出，从而销毁其占用的内存空间。

如果调用栈中记录了较多的上下文，那么就会有可能发生“栈溢出”，消耗大量的
